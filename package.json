{
  "name": "courier-tenant-inbox-test",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Single fetch-only script exercising the Courier tenant inbox flow (no SDK).",
  "scripts": {
    "test": "NODE_OPTIONS=--experimental-vm-modules jest"
  },
  "devDependencies": {
    "jest": "^30.4.2"
  }
}
